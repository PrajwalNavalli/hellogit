---
- name: install nodejs on ubuntu 
  hosts: all
  become: yes
  tasks:
    - name: install nodejs
      ansible.builtin.apt
         name: install nodejs
         upsate_cache: yes
         state: present